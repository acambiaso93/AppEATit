<h1>All Recipes</h1>

<ul>
  <% @recipes.each do |recipe| %>
    <li>
      <%= link_to recipe.name, recipe_path(recipe) %>
    </li>
  <% end %>
</ul>


<%= render partial: 'shared/recipe-card', library: {recipe: @recipe} %>
